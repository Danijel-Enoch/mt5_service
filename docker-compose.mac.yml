# Optional overlay for Apple Silicon Macs (Docker runs arm64 by default).
# MT5/Wine require x86_64 — forces amd64 emulation on Mac only.
#
# Usage: make up-mac

services:
  mt5-worker-1:
    platform: linux/amd64

  mt5-worker-2:
    platform: linux/amd64
